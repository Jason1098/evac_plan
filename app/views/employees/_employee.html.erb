
<td><%= link_to employee.full_name, employee %></td>
<td><%= employee.badge %></td>
<td><%= employee.gender %></td>
<td><%= employee.location.name if employee.location %></td>
<td><%= employee.department.name if employee.department %>
<td><%= employee.muster_location.name if employee.muster_location %></td>
