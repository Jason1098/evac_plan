<%= simple_form_for(@location) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :number %>
    <%= f.input :name %>
    <%= f.input :building_id, collection: @buildings  %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
